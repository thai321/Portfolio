<div class="card">
	<%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
	<p class="card-text">
		<span><%= link_to portfolio_item.title, portfoli_show_path(portfolio_item) %></span><%= portfolio_item.subtitle %>
	</p>
</div>

<!-- 
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
 -->